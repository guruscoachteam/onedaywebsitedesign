{
  "name": "onedaywebsitedesign",
  "private": true,
  "version": "1.0.0",
  "description": "One Day Website Design — landing page and intake form",
  "scripts": {
    "dev": "python3 -m http.server 8080",
    "deploy": "wrangler pages deploy . --project-name=onedaywebsitedesign"
  },
  "dependencies": {
    "stripe": "^18.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.107.0"
  }
}
